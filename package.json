{
  "name": "missing-vowels",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "prepare": "husky",
    "format": "cd frontend && node_modules/.bin/biome format --write src",
    "format:backend": "cd backend && .venv/bin/ruff format app.py categories.py",
    "lint:backend": "cd backend && .venv/bin/ruff check app.py categories.py"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  }
}
